# Install the application's runtime dependencies first.
-r requirements.txt

# Formatting and linting
black==26.5.1
ruff==0.16.1

# Static type checking
mypy==2.3.0

# Testing and coverage
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1

# Local Git hook management
pre-commit==4.6.0

# pytest and related packages are included for future tests. The project
# does not currently require a test suite to install development tools.

# Pre-commit is optional. To enable the local Black and Ruff hooks, run:

#     pip install -r dev-requirements.txt
#     pre-commit install

# To run the checks without enabling a hook:

#     pre-commit run --all-files

# Run mypy manually.